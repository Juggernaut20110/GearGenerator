.venv/
.venv-build/
__pycache__/
*.py[cod]
.pytest_cache/

# Nuitka
build/
dist/
# Written when a Nuitka run dies, including when it dies only because its
# output was piped into something that stopped reading.
nuitka-crash-report.xml

# Local tooling config. Both name absolute paths on one machine - .mcp.json
# points at a checkout of the MCP server elsewhere on the disk - so neither
# is portable enough to be worth carrying in the repo.
.claude/
.mcp.json

# SOLIDWORKS output and backups
*.sldprt
*.sldasm
*.slddrw
*.SLDPRT
*.SLDASM
*.SLDDRW
~$*
out/
