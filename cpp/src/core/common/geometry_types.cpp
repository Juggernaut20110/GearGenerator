#include "core/common/geometry_types.hpp"

namespace geargen::core {

// The first native compilation unit intentionally owns the common geometry
// vocabulary. Most of the types are value-only and inline; this translation
// unit gives the library a stable home for future common geometry helpers.

} // namespace geargen::core
